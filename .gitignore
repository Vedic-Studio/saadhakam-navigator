# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Local data (PII-safe storage for dev)
.data/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Next.js build output (never commit)
.next/

# Build artifacts
temp_cache/
*.tsbuildinfo
__pycache__/
*.pyc

# Local Claude worktree metadata (must never be committed)
.claude/worktrees/
.vercel

# Legacy / dead directories (kept locally, not in repo)
.agent/
.claire/
.roo/
workspace/
frontend/
content-pipeline/

# Note: .agents/ and .claude/skills/ ARE tracked — they hold active skill
# definitions (seo-optimize, answer-engine-optimization, llm-indexing, etc.)
# that inform the Claude Code workflow for this project.

plans/
venv/
.venv/
backend/venv/
backend/.venv/

# Local backend secrets
backend/.env
backend/data/

# Package manager lockfiles (project uses npm)
bun.lockb

# Backlink status reports (generated, ephemeral)
scripts/backlinks/status-*.json
scripts/backlinks/CHECKLIST.md

# Ahrefs audit CSV exports (local working data)
Ahfrens Audit/

# Scheduled tasks lock file (local runtime)
.claude/scheduled_tasks.lock
